export const REPO_TYPES = {
    TagRepo: Symbol("TagRepo"),
    AttractionRepo:Symbol("AttractionRepo")
}

export const SERVICE_TYPES = {
    AttractionService : Symbol("AttractionService"),
    TagService : Symbol("TagService")
}